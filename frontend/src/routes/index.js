/**
 * Route guards and route configuration.
 *
 * Planned guards:
 *   - GuestRoute (redirect authenticated users)
 *   - ProtectedRoute (require authentication)
 *   - RoleRoute (require specific role)
 */
